import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.synclab.smartparking',
  appName: 'smart-parking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
